{
  "name": "welltick-contracts",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network mumbai"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "dotenv": "^16.0.3",
    "hardhat": "^2.17.0",
    "@openzeppelin/contracts": "^4.9.0",
    "typescript": "^5.0.0"
  }
}